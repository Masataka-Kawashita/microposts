     <%= form_for(current_user.favorites_relationships.build, remote: true) do |f| %>
     <div><%= hidden_field_tag :micropost_id, micropost.id %></div>
     <%= f.submit "favorite", class: "btn btn-primary" %>
     
     <%end %>
     